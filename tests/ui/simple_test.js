describe('simple', () => {
  it('should be sane', () => {
    expect(false).not.toBe(true);
  });
});

describe('whatever', () => {
  it('should be eee', () => {
    expect(false).not.toBe(true);
  });
});
